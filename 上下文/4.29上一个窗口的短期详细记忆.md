# 🧠 冰箱侦探 · 短期详细记忆（2026-04-28）

> 用途：跨对话窗口保持连续性
> 使用方式：新窗口时与「项目上下文文档」一起提供给 AI
> 特点：比上下文文档更细节，保留最近决策逻辑与推导过程

---

# 一、本轮核心阶段定位

当前项目处于：

```txt
从“设计完成” → 进入“真正开发启动前的结构搭建阶段”
```

已完成：

- 产品结构（6大模块）✅
- 技术选型（Expo + Supabase + Claude）✅
- 项目初始化（本地项目 + 基础架构）✅
- GitHub 仓库建立 ✅
- GitFlow 协作模型建立（main / develop / feature）✅

未完成但关键：

```txt
❗ Supabase 真实连接（当前最优先）
❗ 数据库结构设计
❗ services / types / prompt 统一规范
```

---

# 二、Git 协作体系（已确定）

## 协作模式

```txt
单一 GitHub 私有仓库 + collaborator 协作
不使用 fork
```

流程：

```txt
GitHub 仓库（唯一中心）
    ↓ clone
每人本地开发
    ↓ push
同一个 GitHub 仓库
```

---

## 分支结构（已确定）

```txt
main      稳定版本（不直接改）
develop   开发主线（不直接改）
feature/* 每个人的功能分支
```

---

## 标准开发流程

### 每次开始开发

```bash
git checkout develop
git pull origin develop
git checkout -b feature/xxx
```

---

### 提交代码

```bash
git add .
git commit -m "feat: xxx"
git push origin feature/xxx
```

---

### 合并流程

```txt
feature → PR → develop
（不直接进 main）
```

---

## 关键协作规则

```txt
1. 不改 main
2. 不改 develop
3. 所有功能走 feature 分支
4. .env 不上传
5. 公共文件改动必须沟通：
   - package.json
   - types
   - lib/supabase.ts
   - supabase/migrations
```

---

# 三、分工方式的重要决策（核心）

## ❌ 直接按功能模块分工（当前阶段不适合）

问题：

- 数据结构会乱
- Supabase 使用方式不统一
- prompt 分散
- 后期无法整合

---

## ✅ 最终采用：混合分工模式

```txt
阶段1：横向搭地基（统一基础）
阶段2：纵向按功能模块开发
```

---

# 四、当前必须先完成的“技术地基”

## 1. Supabase 连接（最高优先级）

必须完成：

```txt
.env 填真实 key
创建 supabase client
前端测试读取数据（health_check）
```

目标：

```txt
Expo 能成功读取 Supabase 数据
```

---

## 2. 数据存储结构（关键理解）

### 数据存在哪里（已统一）

| 数据     | 存储                            |
| -------- | ------------------------------- |
| 健康信息 | SecureStore（本地）→ 注册后同步 |
| 口味偏好 | AsyncStorage / Supabase         |
| 厨房设备 | Supabase                        |
| 调料     | Supabase                        |
| 冰箱食材 | Supabase                        |
| 冰箱照片 | Supabase Storage                |
| 菜谱库   | Supabase                        |
| prompt   | docs + Edge Function            |
| AI调用   | Edge Function                   |

---

## 3. Prompt 管理（关键规范）

```txt
docs/
  └── prompt 说明（人读）

supabase/functions/
  └── 实际运行 prompt
```

---

## 4. services 层（非常关键）

前端不能直接调 Supabase：

```txt
❌ 页面直接写 supabase.from()

✅ 必须走：
services/*.ts
```

结构：

```txt
profileService
fridgeService
recipeService
aiService
```

---

# 五、五大功能模块的数据流（核心理解）

## ① 信息录入

```txt
用户输入
→ 本地存储
→ 注册后进 Supabase
```

---

## ② 冰箱识别

```txt
上传图片
→ Storage
→ Edge Function (Claude)
→ 返回食材 JSON
→ 用户确认
→ 存 fridge_items
```

---

## ③ 推荐菜谱

```txt
fridge_items + preferences + recipes
→ 匹配算法
→ 推荐卡片
```

---

## ④ 教程模块

```txt
选择菜
→ recipe_steps
→ 时间轴展示
```

---

## ⑤ 评价模块（后置）

```txt
上传成品图
→ AI评分
→ 成就报告
```

---

# 六、当前推荐分工（第一轮）

## 你（项目主控）

负责：

```txt
GitFlow
Supabase连接
数据库结构
services
types
README
```

---

## 队友 A（前端）

```txt
信息录入模块 UI
（先 mock，不接 Supabase）
```

---

## 队友 B（功能 UI）

```txt
冰箱识别页面 UI
上传 + 食材卡片
```

---

## 队友 C（内容 / 推荐）

```txt
20-30 道测试菜
标签结构
推荐页面
```

---

# 七、关键认知升级（本轮最重要收获）

## 1️⃣ Stitch ≠ 直接开发结构

```txt
Stitch 是 UI Blueprint，不是工程结构
```

---

## 2️⃣ 功能模块 ≠ 可以独立开发

必须依赖：

```txt
统一数据库
统一 services
统一 AI 接口
```

---

## 3️⃣ 项目成功关键不是“写页面”，而是：

```txt
数据流是否统一
架构是否干净
协作是否稳定
```

---

## 4️⃣ 当前阶段最重要一句话

```txt
先搭地基，再盖房子
```

---

# 八、下一步行动清单（明确）

你接下来应该做：

```txt
1. 完成 Supabase 连接
2. 写 lib/supabase.ts
3. 建 health_check 表并测试
4. 建立 services 结构
5. 建立 types
6. 写 README
7. 定数据库表结构
```

---

# 九、当前项目真实状态总结

```txt
产品设计：100%
UI方向：90%
工程架构：40%
协作体系：80%
后端连通：0% → 即将突破
```

---

# 🔚 一句话总结

```txt
你们已经完成了“想做什么”，
现在进入“怎么让它真的跑起来”的阶段。
```

---
