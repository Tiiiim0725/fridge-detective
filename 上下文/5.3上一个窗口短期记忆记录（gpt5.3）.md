冰箱侦探｜短期记忆（当前窗口总结）
一、当前阶段定位

当前项目已完成：

Supabase 最小连接闭环（health_check）✅

项目状态从：

后端连通 0%

升级为：

前后端最小数据链路已打通 ✅
二、本窗口完成的核心任务（按时间顺序）
1️⃣ Supabase 配置与理解

完成：

理解 Supabase 新版 API key 结构：
使用 Publishable key
不使用 secret key
确认 Project URL 规则：
https://<project-id>.supabase.co
成功填入本地 .env：
EXPO*PUBLIC_SUPABASE_URL=...
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable*...
2️⃣ 本地 Expo 项目结构确认

关键认知建立：

整个本地项目文件夹 = Expo 项目

无需在 Expo 网站创建项目。

3️⃣ 环境变量规范（重要工程规范）

确认：

.env 不上传 GitHub
.env.example 上传 GitHub

原因理解：

避免密钥泄露 + Git 历史不可清除 + 未来扩展风险
4️⃣ Supabase 数据库初始化

完成：

创建表：
health_check

字段：

id int8 primary key
message text (not null)
created_at timestamptz default now()
插入测试数据：
Supabase connected successfully
5️⃣ RLS 权限配置（关键）

完成：

create policy "Allow public read health_check"
on public.health_check
for select
to anon
using (true);

理解：

RLS 开启后必须写 policy，否则查不到数据
6️⃣ 编程 AI 执行（第一段工程代码）

AI完成：

文件结构：
lib/supabase.ts
services/healthCheckService.ts
app/dev-health-check.tsx
架构规则（已落实）：
页面 ❌ 不直接调用 supabase
必须通过 services 层
7️⃣ 最终验证（关键里程碑）

浏览器访问：

http://localhost:8081/dev-health-check

页面显示：

Supabase connected: Supabase connected successfully

✅ 成功

三、当前系统架构（已建立）
UI页面 (app/)
↓
services/（业务逻辑层）✅ 已建立
↓
lib/supabase.ts（底层连接）✅ 已建立
↓
Supabase 数据库 ✅ 已连接

types/（数据结构层）❗️尚未建立
四、本窗口关键工程决策（重要）
决策 1：Supabase key 使用规范
使用 Publishable key（前端）
禁止使用 secret/service_role key
决策 2：强制 services 层
所有数据库访问必须走 services
页面禁止直接写 supabase.from()
决策 3：开发顺序（极其关键）
先横向搭技术地基
再做功能模块

而不是：

❌ 每人独立做页面 + 自己连数据库
决策 4：当前阶段目标
不是做功能
是搭“工程基础设施”
五、核心概念认知（本窗口新建立）
1️⃣ services 是什么
前端里的“后端层”

职责：

所有数据读写
所有 AI 调用
所有业务逻辑入口
2️⃣ types 是什么
数据结构的统一标准（契约）

作用：

统一前端 / services / 数据库的数据结构
防止结构混乱
3️⃣ 架构关系（非常重要）
types：定义数据长什么样
services：定义数据怎么来
六、当前完成度
已完成 ✅
Supabase 连接
表创建
RLS 策略
前端 → services → 数据库闭环
基础架构约束（services 层）
未完成 ❗️（当前重点）
types/ 体系 ❗️
services/ 完整边界 ❗️
数据库完整结构 ❗️
七、当前风险点（非常关键）

如果现在直接开发功能，会发生：

❌ 每个人乱写数据结构
❌ 页面直接连数据库
❌ services 被绕过
❌ 后期无法维护
八、下一步明确任务（当前正在推进）

当前进入：

横向技术地基阶段（Phase 2）
下一步顺序：
1️⃣ 提交 health_check 代码（锁定当前成果）
2️⃣ 建立 types/ 初版（定义数据结构）
3️⃣ 建立 services/ 初版边界（定义接口层）
九、当前进度位置（一句话）
已完成 Supabase 最小闭环验证，正准备进入 types 和 services 的系统化架构搭建阶段。
十、下一个窗口应继续做的事
👉 执行 git 提交（health_check）
👉 设计 types/ 结构（由人主导，不完全交给 AI）
👉 设计 services/ 边界（统一接口规范）

如果下一窗口只读这一份文档，就应该能知道：

现在不是做产品功能
而是在搭整个系统的“骨架”

如果你下一步准备继续，我建议：

👉 下一窗口直接从：

types 架构设计（最关键的一步之一）

开始，而不是继续写代码。
